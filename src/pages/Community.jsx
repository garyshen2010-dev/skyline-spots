import { useState } from "react";import { Link } from "react-router-dom";import { spots } from "../data.js";import "../App.css";
function Community(){const submittedSpots=JSON.parse(localStorage.getItem("submittedSpots"))||[];const allSpots=[...submittedSpots,...spots];const[searchTerm,setSearchTerm]=useState("");const[cityFilter,setCityFilter]=useState("All");const[discoveryFilter,setDiscoveryFilter]=useState("All");const[typeFilter,setTypeFilter]=useState("All");const[sortOption,setSortOption]=useState("Most Upvoted");const cities=["All",...new Set(allSpots.map(s=>s.city))];const discoveryLevels=["All","Hidden Gem","Local Favorite","Popular but worth it","Community Spot"];const spotTypes=["All","Park","Rooftop","Bridge","Parking Garage","Overlook","Street Corner","Restaurant/Patio","Other","Waterfront Park","Observation Deck","Street View","Elevated Park","Lakefront"];const filteredSpots=allSpots.filter(spot=>{const searchText=`${spot.name} ${spot.city} ${spot.description} ${spot.type} ${spot.hiddenLevel} ${spot.tags.join(" ")} ${spot.submittedBy}`.toLowerCase();return searchText.includes(searchTerm.toLowerCase())&&(cityFilter==="All"||spot.city===cityFilter)&&(discoveryFilter==="All"||(spot.hiddenLevel??"Community Spot")===discoveryFilter)&&(typeFilter==="All"||spot.type===typeFilter)}).sort((a,b)=>sortOption==="Most Upvoted"?(b.upvotes??0)-(a.upvotes??0):sortOption==="Highest Rated"?(b.rating??0)-(a.rating??0):sortOption==="Newest"?allSpots.indexOf(a)-allSpots.indexOf(b):0);function clearLocalSubmissions(){localStorage.removeItem("submittedSpots");window.location.reload()}return <div className="app"><nav className="navbar"><h2>SkylineSpots</h2><div className="nav-links"><Link to="/">Home</Link><Link to="/explore">Explore</Link><Link to="/community">Community</Link><Link to="/submit">Submit</Link></div></nav><section className="section community-page"><div className="section-header"><p className="section-label">COMMUNITY FEED</p><h2>Community Skyline Spots</h2><p>Browse skyline spots submitted by users across different cities. Find hidden gems, local favorites, rooftops, parking garages, bridges, and underrated city-view angles.</p></div><div className="search-box"><input placeholder="Search spots, cities, tags, or users..." value={searchTerm} onChange={e=>setSearchTerm(e.target.value)}/></div><div className="filter-panel">{[["City",cities,cityFilter,setCityFilter],["Discovery Level",discoveryLevels,discoveryFilter,setDiscoveryFilter],["Spot Type",spotTypes,typeFilter,setTypeFilter],["Sort By",["Most Upvoted","Highest Rated","Newest"],sortOption,setSortOption]].map(([title,arr,val,set])=><div className="filter-group" key={title}><h3>{title}</h3><div className="filter-buttons">{arr.map(x=><button key={x} className={val===x?"active-filter":""} onClick={()=>set(x)}>{x}</button>)}</div></div>)}</div><div className="feed-toolbar"><p className="results-count">Showing {filteredSpots.length} of {allSpots.length} community spots</p>{submittedSpots.length>0&&<button className="secondary-button" onClick={clearLocalSubmissions}>Clear Local Submissions</button>}</div><div className="community-feed">{filteredSpots.map(spot=><Link to={`/spot/${spot.name.toLowerCase().replaceAll(" ","-")}`} className="community-feed-card" key={`${spot.city}-${spot.name}`}><div className="community-feed-image" style={{backgroundImage:`url(${spot.image})`}}></div><div className="community-feed-content"><div className="community-feed-top"><div><p className="country">{spot.city}</p><h3>{spot.name}</h3></div><div className="feed-upvotes"><p>▲</p><h4>{spot.upvotes??0}</h4></div></div><p>{spot.description}</p><div className="mini-community-row"><p>Submitted by @{spot.submittedBy??"anonymous"}</p><p>{spot.hiddenLevel??"Community Spot"}</p></div><div className="tag-row"><span>{spot.type}</span><span>{spot.bestTime}</span><span>{spot.access}</span>{spot.tags.slice(0,2).map(tag=><span key={tag}>{tag}</span>)}</div></div></Link>)}</div>{filteredSpots.length===0&&<div className="empty-state"><h3>No spots found</h3><p>Try changing your search or filters.</p></div>}</section></div>}
export default Community;
